ALTER	DATABASE dateadan CHARACTER SET utf8 COLLATE utf8_unicode_ci;

DROP TABLE IF EXISTS question;

INSERT INTO question1 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question2 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question3 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question4 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question5 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question6 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question7 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question8 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question9 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);

INSERT INTO question10 (
	questionId BINARY(16) NOT NULL,
	questionContent VARCHAR (128) NOT NULL,
	questionValue TINYINT (1) NOT NULL,
	PRIMARY KEY(questionId)
);